# NestJS 后端开发环境 Dockerfile
# 支持热重载和开发调试

FROM node:18-alpine

# 安装开发工具
RUN apk add --no-cache git curl

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3001
EXPOSE 9229

# 设置环境变量
ENV NODE_ENV=development

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD curl -f http://localhost:3000/health || exit 1

# 开发模式启动（支持热重载）
CMD ["npm", "run", "start:dev"]