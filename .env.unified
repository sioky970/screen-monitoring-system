# 统一Docker Compose环境变量配置
# 复制到.env使用：cp .env.unified .env

# ========================================
# 部署模式配置 (通过COMPOSE_PROFILES控制)
# ========================================
# 开发环境完整版: COMPOSE_PROFILES=dev
# 生产环境完整版: COMPOSE_PROFILES=prod  
# 仅基础设施: COMPOSE_PROFILES=infra
# 开发环境+工具: COMPOSE_PROFILES=dev,tools

# 默认开发环境
COMPOSE_PROFILES=dev

# ========================================
# 数据库配置
# ========================================
# MySQL镜像配置 (使用预构建镜像或官方镜像)
# 预构建镜像: screen-monitor-mysql:1.0.0 (包含完整数据库结构)
# 官方镜像: mysql:8.0 (需要初始化脚本)
MYSQL_CUSTOM_IMAGE=screen-monitor-mysql:1.0.0

# MySQL配置
MYSQL_ROOT_PASSWORD=dev_root_123
MYSQL_DATABASE=screen_monitoring
MYSQL_USER=dev_user
MYSQL_PASSWORD=dev_pass_123

# 端口配置（开发环境使用冷门端口）
MYSQL_PORT=47821

# ========================================
# Redis配置
# ========================================
REDIS_PORT=47822

# ========================================
# MinIO对象存储配置
# ========================================
MINIO_ROOT_USER=devadmin
MINIO_ROOT_PASSWORD=devadmin123
MINIO_DEFAULT_BUCKETS=monitoring-screenshots
MINIO_API_PORT=47823
MINIO_CONSOLE_PORT=47824

# ========================================
# 应用服务配置
# ========================================
# JWT密钥
JWT_SECRET=dev-jwt-secret-key

# 开发环境端口
BACKEND_DEV_PORT=47828
FRONTEND_DEV_PORT=47827
DEBUG_PORT=47829

# 生产环境端口
BACKEND_PORT=47831
FRONTEND_PORT=47830

# Node环境
NODE_ENV=development

# ========================================
# 开发工具端口
# ========================================
ADMINER_PORT=47825
REDIS_GUI_PORT=47826